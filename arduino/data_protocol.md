# Arduino → Unity Data Protocol  
### VR Cricket — Bat Tracking System

This document defines the communication format used by the Arduino (MPU6050) to send bat orientation data to the Unity engine.

---

## 📡 1. Data Format (Serial Output)

The Arduino sends orientation values using the following structure:

